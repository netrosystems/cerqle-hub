# Cerqle Hub instructions

Follow `AGENTS.md` at the repository root. Use `docs/README.md` to locate the authoritative project context, current status, domain specifications and durable decision records. Do not duplicate or override those sources here.
